// Editor-only descriptor for n8n's JWT v1 core node.
// Tokens, keys, credentials, and claims are never parsed, signed, or verified.

const prettifyOperation = (operation) => {
  if (operation === 'sign') return 'Sign a JWT';
  if (operation === 'decode') return 'Decode a JWT';
  if (operation === 'verify') return 'Verify a JWT';
  return operation;
};

const overrideAlgorithms = [
  'ES256',
  'ES384',
  'ES512',
  'HS256',
  'HS384',
  'HS512',
  'PS256',
  'PS384',
  'PS512',
  'RS256',
  'RS384',
  'RS512',
];

const credentialAlgorithms = [
  'HS256',
  'HS384',
  'HS512',
  'RS256',
  'RS384',
  'RS512',
  'ES256',
  'ES384',
  'ES512',
  'PS256',
  'PS384',
  'PS512',
  'none',
];

const jwt = {
  type: 'jwt',
  n8nType: 'n8n-nodes-base.jwt',
  n8nVersion: 1,
  versionHistory: [1],
  label: 'JWT',
  defaultName: 'JWT',
  subtitle: `={{(${prettifyOperation})($parameter.operation)}}`,
  subtitleMap: {
    decode: 'Decode a JWT',
    sign: 'Sign a JWT',
    verify: 'Verify a JWT',
  },
  description: 'JWT',
  category: 'core',
  categories: ['Development'],
  group: ['transform'],
  inputs: ['main'],
  outputs: ['main'],
  usableAsTool: true,
  icon: '/node-icons/jwt.svg',
  n8nIcon: 'file:jwt.svg',
  iconMode: 'image',
  iconAssetType: 'svg',
  iconAssetSize: { width: 200, height: 200 },
  iconAssetSha256: '0f2119c2be2a8badfc1265f70d728003adf6f60bf4cc4b2601eb37603b814ea6',
  aliases: ['Token', 'Key', 'JSON', 'Payload', 'Sign', 'Verify', 'Decode'],
  docs: 'https://docs.n8n.io/integrations/builtin/core-nodes/n8n-nodes-base.jwt/',
  credentialDocs: 'https://docs.n8n.io/integrations/builtin/credentials/jwt/',
  source: {
    commit: '3d68c29b9281f14097aa9f15e01ac0777e538b11',
    path: 'packages/nodes-base/nodes/Jwt/Jwt.node.ts',
    metadataPath: 'packages/nodes-base/nodes/Jwt/Jwt.node.json',
    credentialPath: 'packages/nodes-base/credentials/JwtAuth.credentials.ts',
    iconPath: 'packages/nodes-base/nodes/Jwt/jwt.svg',
    credentialIconPath: 'packages/nodes-base/credentials/icons/jwt.svg',
    subtitleExpression: '={{(${prettifyOperation})($parameter.operation)}}',
  },
  defaults: {
    name: 'JWT',
  },
  credentialRequirements: [
    {
      type: 'jwtAuth',
      name: 'JWT Auth',
      required: true,
      locked: true,
      inert: true,
      schema: {
        keyType: {
          label: 'Key Type',
          value: 'passphrase',
          options: [
            { label: 'Passphrase', value: 'passphrase' },
            { label: 'PEM Key', value: 'pemKey' },
          ],
          description: 'Choose either the secret passphrase or PEM encoded public keys',
        },
        secret: { label: 'Secret', sourceKind: 'password', showWhen: { keyType: ['passphrase'] } },
        privateKey: {
          label: 'Private Key',
          sourceKind: 'password',
          showWhen: { keyType: ['pemKey'] },
        },
        publicKey: {
          label: 'Public Key',
          sourceKind: 'password',
          showWhen: { keyType: ['pemKey'] },
        },
        algorithm: {
          label: 'Algorithm',
          value: 'HS256',
          options: credentialAlgorithms.map((algorithm) => ({ label: algorithm, value: algorithm })),
        },
      },
      simulationNote:
        'The credential schema is source metadata only. Secret, PEM, and algorithm values are never read or used.',
    },
  ],
  params: [
    {
      key: 'credential',
      n8nKey: 'credentials.jwtAuth',
      label: 'Credential to connect with',
      kind: 'select',
      sourceKind: 'credentials',
      value: 'jwtAuth',
      required: true,
      locked: true,
      options: [{ label: 'JWT Auth', value: 'jwtAuth' }],
      simulationNote:
        'Credential selection is locked. This simulation never authenticates or accesses JWT keys.',
    },
    {
      key: 'operation',
      label: 'Operation',
      kind: 'select',
      value: 'sign',
      required: false,
      noDataExpression: true,
      options: [
        { label: 'Decode', value: 'decode', action: 'Decode a JWT' },
        { label: 'Sign', value: 'sign', action: 'Sign a JWT' },
        { label: 'Verify', value: 'verify', action: 'Verify a JWT' },
      ],
    },
    {
      key: 'useJson',
      label: 'Use JSON to Build Payload',
      kind: 'boolean',
      value: false,
      required: false,
      showWhen: { operation: ['sign'] },
      description: 'Whether to use JSON to build the claims',
    },
    {
      key: 'claims',
      label: 'Payload Claims',
      kind: 'collection',
      value: {},
      required: false,
      showWhen: { operation: ['sign'], useJson: [false] },
      addLabel: 'Add Claim',
      fields: [
        {
          key: 'audience',
          label: 'Audience',
          kind: 'text',
          value: '',
          required: false,
          placeholder: 'e.g. https://example.com',
          description: 'Identifies the recipients that the JWT is intended for',
        },
        {
          key: 'expiresIn',
          label: 'Expires In',
          kind: 'number',
          value: 3600,
          min: 0,
          required: false,
          placeholder: 'e.g. 3600',
          description: 'The lifetime of the token in seconds',
        },
        {
          key: 'issuer',
          label: 'Issuer',
          kind: 'text',
          value: '',
          required: false,
          placeholder: 'e.g. https://example.com',
          description: 'Identifies the principal that issued the JWT',
        },
        {
          key: 'jwtid',
          label: 'JWT ID',
          kind: 'text',
          value: '',
          required: false,
          placeholder: 'e.g. 123456',
          description: 'Unique identifier for the JWT',
        },
        {
          key: 'notBefore',
          label: 'Not Before',
          kind: 'number',
          value: 0,
          min: 0,
          required: false,
          description: 'The time before which the JWT must not be accepted for processing',
        },
        {
          key: 'subject',
          label: 'Subject',
          kind: 'text',
          value: '',
          required: false,
          description: 'Identifies the principal that is the subject of the JWT',
        },
      ],
    },
    {
      key: 'claimsJson',
      label: 'Payload Claims (JSON)',
      kind: 'textarea',
      sourceKind: 'json',
      editor: 'json',
      rows: 5,
      value: '{\n  "my_field_1": "value 1",\n  "my_field_2": "value 2"\n}\n',
      required: false,
      showWhen: { operation: ['sign'], useJson: [true] },
      validateType: 'object',
      ignoreValidationDuringExecution: true,
      description: 'Claims to add to the token in JSON format',
      simulationNote: 'JSON is editable text only and is never parsed or validated.',
    },
    {
      key: 'headerClaims',
      label: 'Header Claims (JSON)',
      kind: 'textarea',
      sourceKind: 'json',
      editor: 'json',
      rows: 5,
      value: '{}',
      required: false,
      showWhen: { operation: ['sign'] },
      validateType: 'object',
      ignoreValidationDuringExecution: true,
      description:
        'Custom claims to add to the JWT header, such as a certificate thumbprint (x5t) or key ID (kid). These are merged with the auto-generated header, and values set here take precedence. Useful for building Microsoft Entra certificate client assertions.',
      simulationNote: 'Header JSON is editable text only and is never parsed or merged.',
    },
    {
      key: 'token',
      label: 'Token',
      kind: 'text',
      sourceKind: 'password',
      inputType: 'password',
      value: '',
      required: true,
      showWhen: { operation: ['verify', 'decode'] },
      validateType: 'jwt',
      description: 'The token to verify or decode',
      simulationNote: 'Token text is never decoded, verified, sent, or stored as a credential.',
    },
    {
      key: 'options',
      label: 'Options',
      kind: 'collection',
      value: {},
      required: false,
      addLabel: 'Add option',
      fields: [
        {
          key: 'complete',
          label: 'Return Additional Info',
          kind: 'boolean',
          value: false,
          required: false,
          showWhen: { operation: ['verify', 'decode'] },
          n8nShowWhen: { '/operation': ['verify', 'decode'] },
          description:
            'Whether to return the complete decoded token with information about the header and signature or just the payload',
        },
        {
          key: 'ignoreExpiration',
          label: 'Ignore Expiration',
          kind: 'boolean',
          value: false,
          required: false,
          showWhen: { operation: ['verify'] },
          n8nShowWhen: { '/operation': ['verify'] },
          description: 'Whether to ignore the expiration of the token',
        },
        {
          key: 'ignoreNotBefore',
          label: 'Ignore Not Before Claim',
          kind: 'boolean',
          value: false,
          required: false,
          showWhen: { operation: ['verify'] },
          n8nShowWhen: { '/operation': ['verify'] },
          description: 'Whether to ignore the not before claim of the token',
        },
        {
          key: 'clockTolerance',
          label: 'Clock Tolerance',
          kind: 'number',
          value: 0,
          min: 0,
          required: false,
          showWhen: { operation: ['verify'] },
          n8nShowWhen: { '/operation': ['verify'] },
          description:
            'Number of seconds to tolerate when checking the nbf and exp claims, to deal with small clock differences among different servers',
        },
        {
          key: 'kid',
          label: 'Key ID',
          kind: 'text',
          value: '',
          required: false,
          showWhen: { operation: ['sign'] },
          n8nShowWhen: { '/operation': ['sign'] },
          placeholder: 'e.g. 123456',
          description:
            'The kid (key ID) claim is an optional header claim, used to specify the key for validating the signature',
        },
        {
          key: 'algorithm',
          label: 'Override Algorithm',
          kind: 'select',
          value: 'HS256',
          required: false,
          showWhen: { operation: ['sign', 'verify'] },
          n8nShowWhen: { '/operation': ['sign', 'verify'] },
          options: overrideAlgorithms.map((algorithm) => ({ label: algorithm, value: algorithm })),
          description:
            'The algorithm to use for signing or verifying the token, overrides algorithm in credentials',
        },
      ],
    },
  ],
  unsupportedVisibleTypes: [
    {
      key: 'claimsJson',
      sourceType: 'json',
      normalizedKind: 'textarea',
      reason: 'The catalog has no dedicated JSON parameter control.',
    },
    {
      key: 'headerClaims',
      sourceType: 'json',
      normalizedKind: 'textarea',
      reason: 'The catalog has no dedicated JSON parameter control.',
    },
    {
      key: 'token',
      sourceType: 'string:password',
      normalizedKind: 'text',
      reason:
        'The renderer has no dedicated password control; the field remains inert and carries its source input type as metadata.',
    },
  ],
  simulation: {
    configurationOnly: true,
    authenticates: false,
    readsCredentials: false,
    parsesJson: false,
    parsesTokens: false,
    signsTokens: false,
    verifiesTokens: false,
    decodesTokens: false,
    crypto: false,
    runtime: false,
    network: false,
    voice: false,
  },
  output: {},
};

export default jwt;
