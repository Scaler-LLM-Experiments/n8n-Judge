Use to separate content groups. Plain rule between sections; `label` for an "OR" break between, say, email and SSO; `vertical` between inline meta.

```jsx
<Divider />
<Divider label="OR" />
<span>4.9 rating</span><Divider vertical />/<span>1,240 reviews</span>
```
