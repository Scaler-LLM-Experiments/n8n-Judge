Use only when wait time is unknown and there's nothing to show yet — inline inside a Button while submitting, or centered in a loading panel. If you can show layout, prefer `Skeleton`; if you know progress, use `Progress`.

```jsx
<Spinner />
<Spinner size={16} color="#fff" />   {/* inside a primary button */}
```
