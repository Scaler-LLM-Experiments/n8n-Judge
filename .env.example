# n8n Judge — environment template. Copy to .env (git-ignored) and fill in.

# --- Database (Prisma + pg-boss share this Postgres) -----------------------
# Host port for the docker-compose Postgres. Change it if 5432 is already
# taken on your machine (a native Postgres, or another project's container),
# and keep DATABASE_URL's port in sync.
POSTGRES_PORT="5432"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/judge?schema=public"

# --- Auth (interim email/password before Login-with-Scaler SSO) ------------
AUTH_SECRET="change-me-openssl-rand-base64-32"
# Behind a proxy (Railway, any load balancer) Auth.js must be told to trust the
# forwarded host, or it builds callback URLs from the container's internal
# hostname and login redirects land on localhost. Not needed for local dev.
AUTH_TRUST_HOST="true"
# Only needed if the deployed origin can't be inferred; harmless to set.
# AUTH_URL="https://your-app.up.railway.app"

# --- Claude API ------------------------------------------------------------
ANTHROPIC_API_KEY=""
# Model tiers (swappable). Sonnet for grading and authoring; cheap tier for Ask-AI.
JUDGE_GRADING_MODEL="claude-sonnet-5"
JUDGE_AUTHORING_MODEL="claude-sonnet-5"
JUDGE_ASKAI_MODEL="claude-haiku-4-5-20251001"

# --- Queue -----------------------------------------------------------------
# pg-boss now, sqs later. pg-boss reuses DATABASE_URL.
QUEUE_DRIVER="pgboss"
GRADING_CONCURRENCY="10"

# --- Voice / mascot --------------------------------------------------------
# Iris speaks through ElevenLabs. With the flag off, or with no key, the journey
# still shows every line as a caption and the mascot still reacts, so nothing
# breaks: narration degrades to text, never to a broken screen.
FEATURE_VOICE="false"
ELEVENLABS_API_KEY=""
# The voice to speak as. Find it in ElevenLabs under Voices, "ID" on the card.
ELEVENLABS_VOICE_ID=""
# v3 is the model that understands the inline [tags] in the phrase book
# (apps/web/src/lib/voiceLines.js). Overridable so a model rename does not need
# a code change.
ELEVENLABS_MODEL_ID="eleven_v3"

# --- Pre-rendered voice clips ----------------------------------------------
# Rendering a line on demand costs a vendor round trip, which is the pause you
# hear between a click and Iris speaking. The phrase book is finite and fully
# enumerable, so `npm run voice:generate` renders the lot once and stores it.
#
# Clips are content-addressed by (voice, model, spoken text): no manifest to keep
# in sync, editing one line regenerates only that line, and a re-run is free.
#
#   none   (default) always render live. Fine for a first look, slow in use.
#   local  files under VOICE_CLIP_DIR. Zero infra, baked into the image, no
#          network hop at all. Regenerating needs a deploy.
#   s3     regenerate without shipping code. Credentials come from the standard
#          AWS chain, so only the bucket and region belong here.
VOICE_CLIP_BACKEND="none"
VOICE_CLIP_DIR=".voice-clips"

# S3, when VOICE_CLIP_BACKEND=s3. These are the names the platform team issues,
# and they carry their own key pair: voice clips sit in their own bucket on their
# own credentials, so a mistake here cannot reach anything else. Without the key
# pair the standard AWS chain is used instead, which is what a task role needs.
AUDIO_S3_BUCKET=""
AUDIO_S3_REGION="ap-south-1"
AUDIO_S3_ACCESS_KEY_ID=""
AUDIO_S3_SECRET_ACCESS_KEY=""
# Optional, so clips can share a bucket without colliding.
AUDIO_S3_PREFIX="voice-clips"
