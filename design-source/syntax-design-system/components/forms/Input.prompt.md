Single-line field. Pass `label`, `hint`, and `error`. Brand focus ring, sharp corners.

```jsx
<Input label="Work email" type="email" hint="We’ll send your application link here." />
<Input label="Phone" error="Enter a 10-digit number." icon={<Phone size={16} />} />
```
