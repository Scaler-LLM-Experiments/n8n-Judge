Square identity mark. Pass `src` for a photo or just `name` for initials on a brand tile.

```jsx
<Avatar src="/assets/mentor-sample.png" name="Anshuman Singh" size={48} />
<Avatar name="Priya R" presence="online" />
```

Never round avatars — the brand rule is zero radius everywhere.
