# -*- coding: utf-8 -*-
"""Class_08_Building_Agents_n8n_Zapier.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1y5CcAY5oD3-L4UcAWVo6jlXnSmBbw9-l

# Class 8: Building Agents with n8n & Zapier
### *Narration Script — Instructor Read-Along Edition*

---

> **How to use this script:**  
> Every cell is a self-contained block. Sections marked `[Instructor says]` are your spoken lines — read them naturally, as if talking to a friend.  
> Sections marked `[Action]` mean you do something on screen. Sections marked `[Transition]` are your bridge lines between topics.
> MCQ checkpoints are embedded at key moments — pause the narration and run them interactively.

---

**Tools used today:** n8n (app.n8n.cloud — free trial) · Zapier (zapier.com — free tier) · Google account (Docs / Gmail / Calendar)  

**Today's Learning Journey:**
1. Wiring an **n8n AI Agent node** to real tools and watching the LLM decide what to call
2. Understanding **what counts as a tool** and how the agent autonomously picks them
3. Using **Zapier AI** for simpler trigger-based automations
4. Practical **error handling and retry logic** for production-grade agents
5. Building a **working no-code research agent** from scratch

---
## Section 1 — From Paper Specs to Working Agents
*(~10 minutes)*

---

### [Instructor says]

"Alright everyone, welcome to Class 8! Take a breath — because today is the day things get *real*.

Last class, you learned what an agent *is*. You studied the Think–Act–Observe loop. You mapped out tools, memory, and autonomy. You even designed agent specs on paper — which, honestly, was half the battle.

Today? We build them. For real. Agents that search the web, write to Google Docs, and send emails — all on their own.

Now — and I want to be upfront about this — we are *not* writing any code today. And as engineers, some of you might be thinking: 'Wait, shouldn't we be coding this?' Great instinct. But here's the thing:

The hardest part of building agents isn't writing the code. It's making the *architecture decisions*. Which tools do I give the agent? How do I handle failures? When does the agent run freely vs. when do I need human approval? Those decisions are the same whether you're in a visual canvas or a Python IDE.

Think of today like learning to drive before studying how an internal combustion engine works. We're using n8n and Zapier — two no-code platforms — as our driving school. The design thinking you build today transfers *directly* to the Python agents we'll code in later classes.

So — open your laptops. Let's build."

---

### [Transition]

"But first — *why these two platforms specifically?* There are dozens of automation tools out there. Let me tell you why n8n and Zapier are the ones worth your time."

---

### Why n8n and Zapier?

![Platform Overview](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/191/846/original/1.png?1776767450)

### [Instructor says]

"Think of it like choosing VS Code as your first code editor. It’s popular, versatile, and the workflows you learn transfer easily to other tools. That's exactly what these two do.

**n8n** is open-source and self-hostable. A lot of engineering teams run it internally for AI-powered automations. It gives you the full ReAct agent loop on a visual canvas — which is exactly what we studied in theory last week.

   > *Technical term — 'ReAct loop':* This stands for Reasoning + Acting. It's the core loop where an AI agent thinks about what to do, takes an action, observes the result, and then thinks again. It's like a `while` loop for a brain — it keeps cycling until the job is done.

**Zapier** is the world's most popular automation tool. Its AI features let product teams ship agent-powered workflows *without* waiting for engineering sprints.

Here's the key insight for you as engineers: **understanding no-code agent patterns makes you faster at designing code-based agents later.** Same architecture — different implementation. You'll see that clearly by the end of today."

---

---
## Section 2 — Concept in 5 Minutes: What Counts as a Tool?
*(~5 minutes)*

---

### [Instructor says]

"Before we open n8n, I want to nail down the single most important concept that separates agents from chatbots: **tools**.

Last class, I said tools are the agent's 'hands.' Let's be more precise today. Here's my working definition:

> *A **tool** is any function the LLM can call to interact with the outside world.*

That's it. Clean and simple. A tool takes an input, does something in the real world, and returns an output.

Now here's the part that trips people up — the LLM doesn't *execute* the tool itself. Think of it like this:

You're the project manager. You *decide* what needs to happen. Your backend engineer actually *runs* the code. The LLM is the PM — it decides to call a tool. The platform is the engineer — it actually executes it. The result comes back to the PM, who decides the next step.

This is a critical distinction. The LLM never directly accesses the internet. It never sends an email. It never writes to a database. It *asks the platform to do it*. Which means **you control what the agent can and cannot do** by choosing which tools you connect.

You're the architect. You decide the blueprint. The agent works within it."

---

![Tool Architecture Diagram](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/191/849/original/2.png?1776767775)

---

### [Instructor says]

"Let me show you the full landscape of what can *be* a tool. Look at this table — because every row is something you could plug into your agent today:"

---

| Tool Category | Concrete Examples | SDE Analogy |
|:---|:---|:---|
| **Information** | Web search, Wikipedia lookup, API call | Like calling a GET endpoint |
| **Communication** | Send email, post to Slack, send SMS | Like a notification service |
| **File Operations** | Read/write Google Docs, create PDF | Like a file I/O module |
| **Data** | Query database, read spreadsheet | Like a DB adapter layer |
| **Calendar** | Create event, check availability | Like a scheduling microservice |
| **Code Execution** | Run Python, execute SQL query | Like a sandboxed runtime |
| **Custom** | Any API endpoint wrapped as a tool | Like writing your own SDK method |

---

### [Instructor says]

"Think of tools like apps on your phone. Your brain — the agent — decides which app to open. The app — the tool — does the actual work. You don't re-build Google Maps from scratch every time you need directions. You just call it.

Each tool you connect to an agent is a *capability you're granting*. And every capability you *don't* connect is a boundary you're setting. That design decision is yours to make. The LLM just works with what you give it.

Okay — got it? Good. Because now we're going to see this in action."

---

---
## Quiz1 — Tools & Agent Architecture

> **[Instructor says]** "Before we dive into the demo, let's do a quick check. I'm going to throw a question at you — think it through, don't just guess. There's a lesson inside the answer."

---

**When an AI agent 'calls a tool' — for example, sending an email — which of the following best describes what actually happens?**

- **A)** The LLM directly accesses the Gmail API and sends the email by itself
- **B)** The LLM generates the text of the email; the automation platform (like n8n) executes the actual send
- **C)** The user manually sends the email based on the LLM's suggestion
- **D)** The email is queued inside the LLM's memory and sent when the session ends

---

### Correct Answer: **B**

> **[Instructor says]** "The correct answer is B — and this is one of the most important mental models you'll take from today.
>
> The LLM is the decision-maker, not the executor. It reasons about what needs to happen and *requests* the platform to call the tool. The platform — n8n, Zapier, or your own backend code — is the one that actually runs it and returns the result.
>
> Think of it like a senior engineer raising a Jira ticket. They *specify* what needs to be built. Someone else builds it. Then they review the output. The LLM never has direct internet access or direct API credentials — it's operating through a controlled proxy. That's *by design*, and it's what makes agents safe and controllable.
>
> If you picked A — totally understandable. That's the common misconception. LLMs are powerful, but they're sandboxed. The platform is the gatekeeper."

---

---
## Section 3 — Live Demo: The n8n AI Agent Node
*(~15 minutes)*

---

### [Instructor says]

"Alright — screen share time. This is where last week's diagrams become a real, running system.

We're going to open n8n and I'm going to walk you through every node I place on the canvas. For each one, I'll tell you *why* it's there — not just *what* it is. Pay attention to how each piece maps back to what we've been studying."

---

### What is n8n?

### [Instructor says]

"n8n — pronounced 'n-eight-n' or just 'nodemation' by some — is an **open-source workflow automation platform**. You build workflows by dragging nodes onto a canvas and wiring them together. It has over 400 integrations — Gmail, Slack, databases, APIs — and it has a dedicated **AI Agent node** that implements the ReAct loop natively.

   > *Technical term — 'Open-source':* The entire codebase is public — anyone can read it, modify it, or host it on their own servers. This is why engineering teams love it: no vendor lock-in. You can run n8n on your own infrastructure, customize it, and audit exactly what it's doing.

Think of n8n as a visual IDE for workflows. If your usual IDE has a file tree and a code editor, n8n has a node palette and a canvas. Same concept — different interface.

**Setup:** Head over to [app.n8n.cloud](https://app.n8n.cloud/) and create a free account. No credit card needed for the trial."

---

### [Action] Share your screen showing n8n canvas

---

### Step-by-Step Demo Walkthrough

### [Instructor says — read through each step as you perform it on screen]

---

**Step 1 — Create a new workflow.**

"Click **'+ Add workflow'** from the dashboard. You'll see a blank canvas appear — it's going to feel like an empty whiteboard. That's intentional. We're the architects here, and this is our design surface."

---

**Step 2 — Add a Trigger.**

"Every agent workflow needs a starting gun — something that kicks it off. Click the trigger node and select **'Chat Trigger'** from the AI category.

   > *Technical term — 'Trigger':* In automation, a trigger is the event that starts a workflow. It's like the `main()` entry point in a program — nothing runs until this fires. In our case, the trigger is someone sending a chat message.

This gives us a built-in chat interface inside n8n itself, which makes testing super easy — no external setup needed for now."

---

**Step 3 — Add the AI Agent Node.**

"Click the **+** button after the trigger. Search for **'AI Agent'** and add it. This — right here — is the ReAct loop engine. This single node is doing what we diagrammed all of last class: thinking, acting, observing, and looping.

If last class was reading the design spec, this node is the compiled binary."

---

**Step 4 — Configure the Agent's LLM Brain.**

"Click on the AI Agent node. You'll see connection points at the bottom. Think of these like socket ports on a server:
- **Chat Model** — this is the LLM 'brain' socket. Required.
- **Tools** — this is where we plug in capabilities. This is the power socket.
- **Memory** — this is optional, but I always recommend it.

Click **Chat Model** and add an **GeminiAI Chat Model** node. Enter your API key when prompted.

   > *Technical term — 'API key':* Think of this as a password that proves your identity to the OpenAI service. Without it, the service won't respond to your requests. Treat it like a database password — never hardcode it in public repos."

---

![n8n Agent Node Setup](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/192/798/original/Pi7_image_tool.jpeg?1777269426)

---

**Step 5 — Add Tools.**

"Now for the fun part. Click the **Tools** connector on the Agent node. We're giving this agent its hands.

**Tool 1 — Web Search (SerpAPI)**
Search for **'SerpAPI'** in the tools panel and add it. SerpAPI is a service that gives our agent the ability to run real Google searches.

   > *Technical term — 'SerpAPI':* SERP stands for Search Engine Results Page. SerpAPI is a paid service that programmatically fetches Google search results. It's like wrapping Google Search into a function call — `search('WebAssembly 2026 adoption')` returns the top results.

***Steps to generate SerpAPI's API key:***
1. Search, go and sign in to https://serpapi.com/
2. At time of sign in, it will ask to fill captcha or do a visual test.
3. You will get a confirmation at your email id, make sure you check that.
4. Go to API keys on the left side task bar, and get your free api key, which you will add in n8n.

The agent will see this tool described as: *'Search the web for information on any topic.'* That description matters — and we'll talk about why in a moment.

**Tool 2 — Google Docs**
Search for **'Google Docs'** and add the create-document tool. Connect your Google account when prompted.

The agent sees this as: *'Create a new Google Doc with a title and content.'*

Notice something: neither of these tools has code in them. They're configurations. The platform handles all the API communication under the hood."

---

**Step 6 — Add Memory.**

"Click the **Memory** connector and add **'Simple Memory'**. Set the window size to 10 messages.

   > *Technical term — 'Simple Memory':* Imagine your agent can only see the last N messages in a conversation — like a sliding window over a log file. Setting 10 messages means it remembers the last 10 exchanges. This prevents the agent from 'forgetting' what it was doing mid-task, while keeping the memory footprint manageable.

Without memory, each loop iteration is stateless — the agent forgets what it already searched for. With memory, it builds on previous steps. Huge difference."

---

**Step 7 — Set the System Prompt.**

"In the AI Agent node settings, find the **'System Message'** field. This is the agent's standing instructions.

   > *Technical term — 'System Message' / System Prompt:* Think of this as the README file for your agent's brain. It's a set of instructions that the LLM reads before every conversation — defining its role, rules, and behavior. Unlike user messages, the system message is always there, invisible to the end user. It's like the config file that governs how a service behaves.

This is exactly what we covered in Class 3's prompting section. But now — the agent can actually *act* on these instructions. It's not just generating text anymore."

```
You are a research assistant agent

When given a topic, follow these steps:
1. Search the web for recent, high quality information
2. read and analyse search results
3. Write a clear and structured summary with:
 - A 2-3 sentence overview
 - 3-5 key findings with source attribution

Rules:
- Always site your sources.
- If research results are thin, say so honestly
- Never make up facts
- keep summaries under 500 words
```

---

![n8n System Message Setup](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/192/799/original/2.jpeg?1777270153)

---

**Step 8 — Test It.**

"Click **'Chat'** in the top bar to open the built-in chat panel. Type this:"

```
Research the current state of WebAssembly adoption in 2026.
Save your findings to a Google Doc.
```

### [Action] Run the workflow and watch the execution panel live

### [Instructor says]

"Watch what happens in the execution panel on the right. You'll see the ReAct loop play out in real time:

```
THINK   →  'I need to search for WebAssembly adoption in 2026'
ACT     →  [Calls Web Search tool with query]
OBSERVE →  Gets search results back
THINK   →  'I have enough info to write a summary. Create the doc.'
ACT     →  [Calls Google Docs tool to create document]
OBSERVE →  'Document created successfully'
DONE    →  Responds with summary + doc link
```

You're seeing the loop we drew on a whiteboard last week — now running on real data, in real-time, producing real outputs.

In a code-based agent, you'd have to build this logging yourself. Here, n8n gives it to you for free. That's your debugging superpower.

![image](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/196/996/original/1.png?1778757805)

And here's the thing I want you to appreciate: **no code was written**. Everything you just watched happened because you connected nodes on a canvas and gave the agent clear tools and clear instructions. That's the power of architecture thinking over syntax knowledge."

---

---
## Quiz 2 — The n8n Agent & ReAct Loop

> **[Instructor says]** "Quick pause before you get your hands on the keyboard. Let's make sure the demo landed correctly."

---


**In the n8n AI Agent node, the 'System Message' field serves which primary purpose?**

- **A)** It stores the results of all tool calls made during the agent's run
- **B)** It defines the agent's standing instructions — its role, rules, and behavior — that persist across every interaction
- **C)** It is the text the user sends to the agent to start a task
- **D)** It controls which LLM model (GPT-4 vs Gemini) the agent uses

---

### Correct Answer: **B**

> **[Instructor says]** "Exactly — B is right. The System Message is the agent's permanent configuration layer. A good analogy: imagine you're on-boarding a new engineer to your team. You give them a document that says: 'Here's how we do things — our coding standards, our escalation process, what you should never do without approval.' That's your system message.
>
> It's not what the user says — that's the input. It's not where results are stored — that's the memory. It's the invisible rulebook the agent carries into every conversation.
>
> And we'll see shortly just how powerful — and how important — getting this right is when things go wrong."

---

---
## Section 4 — Hands-On: Build Your Own Agent
*(~25 minutes)*

---

### [Instructor says]

"Alright — your turn. Close your eyes for a second and think about what you just watched. An AI agent that searches the web, synthesizes findings, and writes a professional document — all from a single chat message.

Now — imagine you could build *that* in the next 15 minutes. Because you can. Let's go."

---

### Part A: Build Your n8n Research Agent
*(~15 minutes)*

### [Instructor says]

"Open [app.n8n.cloud](https://app.n8n.cloud/) and replicate what you just saw. Here's your target workflow:"

---

```
YOUR WORKFLOW BLUEPRINT:

  [Chat Trigger] ──► [AI Agent Node] ──► (response in chat)
                           │
                    Connected sockets:
                    ├── Chat Model: Gemini
                    ├── Tool: Web Search (SerpAPI)
                    ├── Tool: Google Docs (create document)
                    └── Memory: Simple (10 messages)
```

---

**Test Prompts — run these one at a time:**

1. `Research the top 3 AI coding assistants in 2026 and compare their features. Save to a Google Doc.`
2. `What are the biggest changes in the Python 3.14 release? Save a summary to Google Docs.`

---

**What success looks like:**
- The execution log shows a web search tool call (agent actually searched — not hallucinated)
- The summary is structured, not just raw search results copy-pasted
- A Google Doc appears in your Drive with real content
- The agent tells you what it found and where it saved it

---

**Troubleshooting Guide:**

| Problem | What to Check |
|:---|:---|
| Agent doesn't search | Is the web search tool wired to the Agent's *Tools* input — not the Model input? |
| Google Docs fails | Did you complete the Google account OAuth connection inside the node? |
| Output is messy | Add more specific formatting instructions to your system message |
| Agent loops forever | We'll fix this in Section 5 — hold tight |

---

> **Beginner tip:** Build incrementally. Get the Chat Trigger + AI Agent + Chat Model working first — just text output, no tools. Then add the search tool. Then add Google Docs. Test after *each* addition. This is exactly how you'd do incremental integration testing in a backend system.

> **Intermediate tip:** After each run, click through the execution panel and trace every tool call. Which query did it search for? What exactly did it send to Google Docs? This is your real-time observability layer — use it.

---

### Exercise 4.2 — Extend with a Third Tool

### [Instructor says]

"Great — basic agent is working. Now let's extend it. This is where the architecture thinking gets interesting: you're choosing a *new capability* to grant. Each tool you add is a new 'service' your agent can call. Choose one that fits how you actually work:"

---

| Tool to Add | What It Enables | How to Find It in n8n |
|:---|:---|:---|
| **Gmail** | Agent sends the research summary via email | Search 'Gmail', use 'Send Email' action |
| **Google Calendar** | Agent schedules a follow-up reminder | Search 'Google Calendar', use 'Create Event' |
| **Slack** | Agent posts findings to a team channel | Search 'Slack', use 'Send Message' |
| **Notion** | Agent creates a page in your knowledge wiki | Search 'Notion', use 'Create Page' |

---

**Test prompt after adding the tool:**

```
Research the latest trends in edge computing.
Save a summary to a Google Doc AND email it to me at [your email].
```

### [Instructor says]

"Watch carefully: the agent should autonomously decide to use *both* the Google Docs tool *and* the Gmail tool — without you specifying the order. It figures out the sequence by reading your goal and matching it to available tools.

That's the autonomy pillar from Class 7 in action. You defined the goal. The agent picked the tools and sequenced them. You didn't orchestrate — you *architected*. Big difference."

---

---
### Part B: Zapier AI Agents — The Quick-Win Alternative
*(~10 minutes)*

---

### [Transition]

"Alright — let me introduce you to a different tool now. Think of n8n as your full-stack framework — powerful, flexible, developer-friendly. And think of Zapier as a managed cloud service — faster to ship, slightly less control, but incredibly powerful for the right use cases."

---

### [Instructor says]

"n8n gives you the full ReAct agent loop with maximum control. But sometimes you don't need all that. Sometimes you just want a simple AI-powered automation that triggers on an event and handles a task without a full agentic loop. That's Zapier's sweet spot.

**What is a Zapier AI Agent?**

Zapier's AI features let you build **trigger-based automations** where an LLM handles one 'thinking' step in an otherwise fixed pipeline.

   > 💡 *Technical term — 'Trigger-based automation':* The workflow only runs when a specific event happens — a new email arrives, a form is submitted, a calendar event starts. It's like an event listener in frontend code: `addEventListener('newEmail', handleEmail)`. Zapier registers these listeners for you.

The critical difference from n8n:"

---

![Zapier vs n8n Comparison](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/192/904/original/3.jpeg?1777279423)

---

| | n8n AI Agent | Zapier AI Automation |
|:---|:---|:---|
| **Loop** | Full ReAct loop — agent decides everything | Fixed pipeline — AI handles one step |
| **Tool selection** | Agent picks tools dynamically | You pre-define the steps |
| **Best for** | Complex, multi-step research tasks | Consistent, repeatable automations |
| **Setup time** | More configuration | Faster — guided setup |
| **SDE analogy** | Writing a custom service | Using a managed cloud service |

---

#### Zapier AI Email Triage — Complete Setup Guide


**Part 0 — Get your free Gemini API key**

1. Go to **aistudio.google.com/apikey**
2. Log in with your Google account
3. Click **Create API key**
4. Select or create a Google Cloud project (free)
5. Copy the key and save it — you will need it in the next part

---

**Part 1 — Connect Gemini to Zapier**

1. Go to **zapier.com/app/connections**
2. Click **+ Add connection**
3. Search for **Google AI Studio (Gemini)**
4. Click **Add connection**
5. Paste your API key when prompted
6. Click **Yes, Continue** — connection is now saved

---

**Step 1 — Trigger: New Gmail email**

1. Go to zapier.com → click **Create → Zaps**
2. Click the trigger block → search **Gmail**
3. Event: **New Email**
4. Connect your Gmail account
5. Set **Label/Mailbox** to `INBOX`
6. Click **Continue** → **Test trigger**
7. Confirm you can see these fields in the sample output — you will need them later:
   - `Body Plain`
   - `Subject`
   - `From Email`

---

**Step 2 — AI classification: Google AI Studio (Gemini)**

1. Click **+** to add a step → search **Google AI Studio (Gemini)**
2. Event: **Send Prompt**
3. Select your connected Gemini account
4. Fill in the fields exactly as follows:

**Model**
```
gemini-2.5-flash
```

**Max Output Tokens**
```
1024
```

**Temperature**
```
0.0
```

**System Instructions**
```
You are an email classifier. You only respond with raw JSON, no explanation, no markdown, no code fences.
```

**Prompt** — paste this, then click into the field and insert the Gmail `Body Plain` dynamic variable at the end where it says [EMAIL BODY HERE]:
```
Classify the email below. Respond with ONLY this JSON on one line, no spaces:
{"category":"BUG_REPORT","urgency":"HIGH"}

Categories: BUG_REPORT, FEATURE_REQUEST, QUESTION, COMPLAINT
Urgency: LOW, MEDIUM, HIGH

Rules:
- Broken things, errors, crashes = BUG_REPORT
- Requests for new features = FEATURE_REQUEST
- General questions = QUESTION
- Dissatisfaction or frustration = COMPLAINT
- HIGH if blocking, data loss, outage, or legal

Email:
[EMAIL BODY HERE]
```

5. Click **Test step**
6. Confirm the output field **Candidates Content Parts Text** shows a complete one-liner like:
```
{"category":"QUESTION","urgency":"LOW"}
```

> **If the JSON is cut off** (e.g. `{"category":` only), increase Max Output Tokens to **200** and test again.

---

**Step 3 — Parse the JSON: Code by Zapier**

Gemini returns a text string. This step converts it into two separate fields — `category` and `urgency` — that the Paths step can filter on.

1. Click **+** → search **Code by Zapier**
2. Event: **Run Javascript**
3. Under **Input Data**, add one entry:
   - Key: `gemini_response`
   - Value: select **Candidates Content Parts Text** from the Gemini step dropdown
4. Paste this into the **Code** box:

```javascript
const raw = inputData.gemini_response.trim();
const clean = raw.replace(/```json|```/g, '').trim();
const parsed = JSON.parse(clean);

return {
  category: parsed.category.toUpperCase().trim(),
  urgency: parsed.urgency.toUpperCase().trim()
};
```

5. Click **Test step**
6. Confirm you see two clean output fields:
   - `category` → e.g. `QUESTION`
   - `urgency` → e.g. `LOW`

> **If you see `SyntaxError: Unexpected end of JSON input`** — go back to the Gemini step and increase Max Output Tokens to 200, re-test the Gemini step, then re-test this Code step.

---

**Step 4 — Paths by Zapier**

1. Click **+** → search **Paths by Zapier** → select it
2. You will see **Path A** and **Path B** by default
3. Click **Add Path** to create a third path



*Path A — Bug reports*

1. Click **Path A** → rename it to `Bug Report`
2. Under Rules, click **Add rule**:
   - Field: `category` (from the Code step)
   - Condition: **exactly matches**
   - Value: `BUG_REPORT`
3. Inside Path A, click **+** → search **Gmail** → Event: **Send Email**
4. Fill in:
   - **To**: insert the `From Email` variable from the Gmail trigger step
   - **Subject**: type `[BUG_REPORT] ` then insert the Gmail `Subject` variable
   - **Body**:
     ```
     Your email has been classified.

     Category: BUG_REPORT
     Urgency: [insert Code urgency variable]

     Original email:
     [insert Gmail Body Plain variable]
     ```



*Path B — Feature requests*

1. Click **Path B** → rename it to `Feature Request`
2. Under Rules, add rule:
   - Field: `category`
   - Condition: **exactly matches**
   - Value: `FEATURE_REQUEST`
3. Inside Path B: Gmail → Send Email
   - **To**: `From Email` variable from trigger
   - **Subject**: `[FEATURE_REQUEST] ` + Gmail Subject variable
   - **Body**: same format as Path A, change Category line to `FEATURE_REQUEST`



*Path C — Urgent complaints*

1. Click the third path → rename it to `Urgent Complaint`
2. Under Rules, add **two rules** connected by **AND**:
   - Rule 1: `category` → **exactly matches** → `COMPLAINT`
   - Rule 2: `urgency` → **exactly matches** → `HIGH`
3. Inside Path C: Gmail → Send Email
   - **To**: `From Email` variable from trigger
   - **Subject**: `[URGENT_COMPLAINT] ` + Gmail Subject variable
   - **Body**: same format as Path A, change Category line to `URGENT_COMPLAINT`

---

**Step 5 — Test the full Zap end to end**

1. Go back to Step 1 (Gmail trigger) → click **Test trigger** to load a real sample email
2. Click **Test step** on each step in order:
   - Gemini step → confirm full JSON in output
   - Code step → confirm `category` and `urgency` are clean strings
   - Paths step → open the matching path and click **Test**
3. Check your inbox — you should receive the email back with the category tag in the subject line
4. Once all steps are green, click **Publish** in the top right

---


**Troubleshooting**

| Problem | Fix |
|---|---|
| `SyntaxError: Unexpected token` | Gemini added extra text around JSON — add `"Return ONLY the raw JSON, no extra text"` to System Instructions |
| Path condition never matches | Confirm Code step output shows all-caps values. The `.toUpperCase().trim()` in the code handles this |
| Gmail Send Email asks for re-authentication | Reconnect your Gmail account in zapier.com/app/connections |
| QUESTION emails go nowhere | Add a 4th path: `category` exactly matches `QUESTION` → Send Email with subject tag `[QUESTION]` |
| Paths not available in your account | Paths requires Zapier Professional plan or higher |

 [Instructor says]

>"Success looks like: you send a test email, the AI classifies it, and the right Slack channel gets notified — all automatically.
>
>Now — is this technically a full agent? No. There's no dynamic tool selection, no loop, no autonomy in the agent sense. But it's the pattern that most teams ship *first*, because it's reliable, predictable, and incredibly valuable.
>
>Think of it like this: a microservice doesn't need to be a full distributed system to be useful. Sometimes a well-designed endpoint is exactly what the job calls for.
>
>Zapier's automation is that endpoint. n8n's agent is the full system. You now know when to use each."

The final Zapier Workflow would look like this:

![image](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/199/387/original/1.png?1779797639)

---
## Quiz 3 — n8n vs Zapier: Choosing the Right Tool

> **[Instructor says]** "You've now seen both platforms in action. Let's test your judgment — because in the real world, picking the right tool is half the job."

---

**Your team wants to build an automation that: (1) monitors a Gmail inbox, (2) uses AI to decide the priority of each new email, and (3) always sends a fixed Slack notification with the priority label. Which platform and approach is the BEST fit?**

- **A)** n8n AI Agent — because it has a full ReAct loop and can handle more complex tasks
- **B)** Zapier AI Automation — because the workflow structure is fixed and the AI only needs to handle one classification step
- **C)** Neither — this task requires writing custom Python code to be reliable
- **D)** n8n AI Agent — because Zapier cannot connect to Gmail

---

### Correct Answer: **B**

> **[Instructor says]** "B is exactly right — and the reasoning matters as much as the answer.
>
> This task has a *fixed, predictable structure*: new email → classify → notify. The AI is doing one job — classification — in an otherwise deterministic pipeline. That's Zapier's wheelhouse. You don't need dynamic tool selection. You don't need a loop. You need a reliable, fast, trigger-based automation.
>
> Using a full n8n ReAct agent here would be like spinning up a Kubernetes cluster to serve a static HTML page. Technically possible — wildly over-engineered.
>
> A is wrong — not because n8n couldn't do it, but because it's the wrong tool for this shape of problem.
> D is wrong — Zapier absolutely connects to Gmail. That's one of its most popular integrations.
>
> The engineering instinct here: match your complexity to your problem's complexity. No more, no less."

---

---
## Section 5 — Error Handling & Retry Logic
*(~20 minutes)*

---

### [Instructor says]

"Here's an important engineering truth that every production system has taught me: *the happy path is the easy part.* Anyone can build an agent that works when everything goes perfectly. The real skill — the thing that separates a demo from production software — is what happens when things go wrong.

And in agentic systems, things *will* go wrong. APIs time out. Search results come back empty. The LLM misreads a tool description and calls the wrong one. You hit a rate limit at the worst possible moment. These aren't edge cases — they're Tuesday afternoon.

So let's talk about the five failure modes you'll encounter, and how to engineer your agent to handle each one."

---

![5 Failure Modes Diagram](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/192/905/original/4.jpeg?1777279561)

---

### The 5 Failure Modes of Agentic Loops

| # | Failure Mode | What Happens | SDE Analogy |
|:---:|:---|:---|:---|
| 1 | **Infinite Loop** | Agent keeps calling tools and never stops | A `while(true)` with no break condition |
| 2 | **Tool Timeout** | An API call hangs or returns an error | A network request with no timeout set |
| 3 | **Empty Results** | Search returns nothing; agent makes things up | A DB query that returns null, but the code assumed a row |
| 4 | **Wrong Tool Selection** | Agent calls the wrong tool due to vague descriptions | Wrong function called because the docstring was ambiguous |
| 5 | **Hallucinated Actions** | Agent *invents* a tool call to a tool that doesn't exist | Calling `undefined` in JavaScript |

---

### [Instructor says]

"Now let's add three safeguards to your n8n research agent. These are your production-grade guardrails. Think of them like seat belts — you hope you never need them, but you'd never drive without them."

---

### Exercise 5.1 — Add Error Handling to Your n8n Agent

### [Instructor says — walk through each safeguard step on screen]

---

**Safeguard 1 — Max Iterations**

"Click on your AI Agent node → Settings → find **'Max Iterations'** → set it to **10**.

   > *Technical term — 'Max Iterations':* This is a hard cap on how many times the agent can loop through Think→Act→Observe before it's forced to stop. It's the equivalent of a circuit breaker in distributed systems — after N failures or attempts, stop and fail gracefully instead of cascading. Without this, a confused agent could run indefinitely and rack up API costs.

After 10 tool calls, the agent stops and returns whatever it has. 10 is a generous limit for research tasks — most finish in 3–5."

---

**Safeguard 2 — Retry on Fail**

"Click on each tool node (Web Search, Google Docs) → Settings → **'Retry on Fail'** → set retries to **3**, wait to **2000ms**.

   > 💡 *Technical term — 'Retry with Exponential Backoff':* When a service call fails, wait a bit and try again — but increase the wait time with each retry. If retry 1 waits 2 seconds, retry 2 waits 4, retry 3 waits 8. This prevents hammering an already-struggling service. It's a foundational pattern in any resilient backend system.

Transient failures — a momentary network blip, a rate limit window — are incredibly common with external APIs. Retries handle them silently, without the user ever noticing."

---

**Safeguard 3 — System Message Guardrails**

"Add these rules to your agent's System Message. Think of each line as a defensive programming comment — but for the LLM:"

---
"""

# ===================================================================
# PASTE THESE RULES INTO YOUR n8n AGENT'S SYSTEM MESSAGE
# (This code cell is for reference — the actual edit is in n8n)
# ===================================================================

# ERROR HANDLING RULES:
# - If web search returns no relevant results, tell the user honestly.
#   Do NOT make up information to fill the gap.
#
# - If a tool call fails after retries, skip that step and explain
#   what happened in your response.
#
# - If you are unsure which tool to use, prefer web search as a
#   safe default.
#
# - Maximum 8 tool calls per task. If you haven't finished by then,
#   return what you have and explain what's still missing.
#
# - Never send emails or create documents unless the user explicitly
#   asked for it.

# [Instructor says]: "Each of these rules handles a specific failure mode.
# 'Do NOT make up information' → handles hallucinated results.
# 'Never send emails unless asked' → prevents unsolicited side effects.
# 'Maximum 8 tool calls' → your software limit inside the LLM's reasoning.
# This is prompt engineering as defensive programming."

"""---

### Exercise 5.1 — Test the Edge Cases

### [Instructor says]

"Now we're going to deliberately break our agent — in a controlled way. This is like writing unit tests for failure scenarios. Great engineers test the unhappy path *before* deployment, not after an incident."

---

**Test 1 — Empty Results (Hallucination Risk)**
```
Research the internal quarterly revenue of a fictional company
called "Nexolith Systems" founded in 2025.
```
> **Expected:** The agent says it couldn't find information. It does NOT invent revenue figures.  
> **If it fails:** Your 'do not make up information' system message rule isn't strong enough. Make it more explicit.

---

**Test 2 — Infinite Loop Risk (Overly Broad Request)**
```
Research everything about AI.
```
> **Expected:** The agent scopes the task down or hits max iterations and stops gracefully.  
> **If it fails:** Lower your Max Iterations setting and add 'If the topic is too broad, ask the user to narrow it down.'

---

**Test 3 — Unintended Side Effects (Tool Overreach)**
```
What's the latest news about Kubernetes?
```
> **Expected:** Agent answers in chat only. No Google Doc is created. No email sent.  
> **If it fails:** The 'never create documents unless asked' rule is missing or too weak.

---

### [Instructor says]

"If all three tests pass, your agent is handling the real world — not just the demo scenario. That's the bar for production. Notice we didn't add more *features* to pass these tests — we added better *constraints*. That's a powerful insight: **a better-constrained agent is a more capable agent.** Counterintuitive, but true."

---

### Exercise 5.2 — The Retry Pattern, Explained

### [Instructor says]

"Let me show you the retry pattern visually before we move on. This is one of those patterns that shows up everywhere — in backend services, in distributed systems, and now in agent loops. Once you see it, you can't unsee it."

---

![Exponential Backoff Retry Pattern](https://d2beiqkhq929f0.cloudfront.net/public_assets/assets/000/192/917/original/5.jpeg?1777283173)

---

### [Instructor says]

"Here's the pattern in plain English:

- Tool call fails → wait 2 seconds → try again
- Fails again → wait 4 seconds → try again
- Fails again → wait 8 seconds → try again
- Still failing → give up and report the error gracefully

Each wait is double the last. Why? Because if a service is struggling, hammering it immediately just makes it worse. You give it breathing room. This is **exponential backoff** — the same algorithm used in TCP congestion control, AWS SDK retries, and every serious production system.

In n8n, the 'Retry on Fail' setting does this for you. But understanding *why* it works prepares you to implement it yourself in code — which is exactly what we'll do in later classes.

   > *Technical term — 'Exponential Backoff':* A retry strategy where the wait time between attempts doubles with each failure. Example: wait 1s, then 2s, then 4s, then 8s. This prevents overwhelming a struggling service while still recovering gracefully from transient failures. Used in everything from Wi-Fi reconnection to cloud API clients."

---

---
## Quiz 4 — Error Handling & Retry Logic

> **[Instructor says]** "Production thinking time. This one tests whether you understand *why* these patterns exist — not just *what* they are."

---

**An agent is running a research task and the web search tool keeps returning HTTP 429 errors ('Too Many Requests'). Without any error handling configured, what is the MOST LIKELY outcome?**

- **A)** The agent automatically switches to a different search provider
- **B)** The agent stops immediately and shows a clear error message to the user
- **C)** The agent retries the same tool call repeatedly until it hits the max iterations limit, potentially wasting API credits and time
- **D)** The agent skips the web search and generates a summary from its training data

---

### Correct Answer: **C**

> **[Instructor says]** "C is correct — and it's an expensive mistake if it happens in production.
>
> Without retry logic with backoff, the agent sees the 429 error, registers 'tool call failed,' and tries again immediately — only to get another 429. It does this over and over until it exhausts its max iteration budget. Meanwhile, you're burning through API credits and the user is waiting.
>
> The right pattern: wait before retrying, increase the wait time exponentially, and cap the number of retries. This is why we configure 'Retry on Fail' with a wait interval — not just a retry count.
>
> A is wrong — agents don't auto-switch providers unless you've explicitly built that logic.
> B would be ideal behavior, but it doesn't happen automatically without configuration.
> D is a hallucination risk — the agent generating from memory instead of searching is exactly the failure mode we're protecting against with the 'do not make up information' rule.
>
> This is why error handling isn't an afterthought. It's a first-class design requirement."

---

---
## Section 6 — Mini Build: Production-Grade Research Agent
*(~25 minutes)*

---

### [Instructor says]

"We've covered tools. We've covered the ReAct loop. We've covered Zapier. We've covered error handling. Now let's tie everything together into something you'd actually be comfortable deploying — and showing to your team.

This is your capstone build for today. Your mission: take your research agent and upgrade it to production-grade. Not a demo. Not a prototype. A system you'd trust to run unsupervised.

Here's the full spec:"

---

### Your Production Agent Spec

```
╔══════════════════════════════════════════════════════════╗
║         RESEARCH AGENT — PRODUCTION SPEC                 ║
╠══════════════════════════════════════════════════════════╣
║  TRIGGER: Chat message with a research topic             ║
║                                                          ║
║  TOOLS:                                                  ║
║  ├── Web Search (find information)                       ║
║  ├── Google Docs (save output)                           ║
║  └── [Your choice: Gmail / Slack / Calendar / Notion]    ║
║                                                          ║
║  SYSTEM MESSAGE includes:                                ║
║  ├── Research methodology (search → analyse → summarise) ║
║  ├── Output format (structured sections with headers)    ║
║  ├── Source citation rules                               ║
║  ├── Error handling instructions                         ║
║  └── Boundaries (what NOT to do)                         ║
║                                                          ║
║  SAFEGUARDS:                                             ║
║  ├── Max iterations: 10                                  ║
║  ├── Retry on fail: 3 attempts per tool                  ║
║  ├── Memory: Window buffer (10 messages)                 ║
║  └── System message guardrails                           ║
║                                                          ║
║  SUCCESS CRITERIA:                                       ║
║  ├── Agent searches the web (verified in execution log)  ║
║  ├── Summary is structured and cites sources             ║
║  ├── Google Doc created with proper title + content      ║
║  ├── Third tool fires when appropriate                   ║
║  ├── Edge cases handled gracefully                       ║
║  └── Agent stays within iteration limits                 ║
╚══════════════════════════════════════════════════════════╝
```

---

### [Instructor says]

"Notice how this spec maps directly to what we studied in Class 7:
- Goal → System Message
- Tools → Connected nodes  
- Memory → Window buffer
- Guardrails → Safeguard settings + system message rules

The paper spec from last class IS the n8n configuration. You've been designing agents for a week — you just didn't know it yet."

---
"""

# ===================================================================
# TEST PROMPTS FOR YOUR PRODUCTION RESEARCH AGENT
# Run these one at a time in your n8n Chat Trigger interface.
# Each tests a different scenario — standard, niche, and edge case.
# ===================================================================

test_prompts = [

    # Test 1: Standard research task — should work flawlessly
    """Research the current state of quantum computing in 2026.
    Focus on practical applications, not theory.
    Save to a Google Doc and email me a summary.""",

    # Test 2: Niche topic with limited public info — tests honest uncertainty
    """Research recent academic papers about using AI agents
    for automated database schema migration.
    Save whatever you find to a Google Doc.""",

    # Test 3: Deliberately vague — tests scope management and iteration limits
    "Tell me everything about machine learning.",
    # (Production agent should scope this down, not loop forever)
]

for i, prompt in enumerate(test_prompts, 1):
    print(f"═" * 60)
    print(f"TEST {i}:")
    print(f"{prompt.strip()}")
    print()

"""---

### [Instructor says]

"After running all three tests, go through this self-assessment. Be honest with yourself — these are the same questions you'd ask in a production readiness review:"

---

```
SELF-ASSESSMENT CHECKLIST:

  [ ] Does your agent search the web in every test?
      (Check the execution log — don't just trust the output text)

  [ ] Are the summaries structured and source-cited?
      (Not just raw search results pasted together)

  [ ] Does Google Docs integration work reliably?
      (Check your actual Google Drive — did the doc appear?)

  [ ] Does the agent handle the niche topic gracefully? (Test 2)
      (Says 'I couldn't find much' rather than fabricating)

  [ ] Does the agent scope down the broad request? (Test 3)
      (Doesn't loop endlessly — picks a reasonable scope and stops)

  [ ] Does your third tool fire when relevant?
      (Try asking it to email you — does it use Gmail?)

  [ ] Did any test hit the max iteration limit?
      (If yes — was the response still graceful?)

  [ ] Would you trust this agent to run without supervision?
      (The ultimate production readiness question)
```

---

> **Beginner note:** If you're still getting the basics working — that's completely fine. Focus on getting Test 1 running reliably. The error handling refinements are layer 2. Build the foundation first.

> **Advanced extension:** In real production, you'd add a dedicated error-handling *workflow* — a separate n8n workflow that triggers when any node fails and logs the error to a monitoring system (like a Slack #alerts channel or a logging database). n8n supports this natively through error workflows. That's your bonus challenge.

---

---
## Section 7 — Wrap-Up & What You Built Today
*(~10 minutes)*

---

### [Instructor says]

"Alright — let's land the plane. Look at what you actually built today. Not what you *studied* — what you *built*."

---

| Skill | What You Can Do Now |
|:---|:---|
| **n8n AI Agent** | Wire an LLM brain to real tools on a visual canvas and watch the ReAct loop run live |
| **Tool Design** | Understand what makes a tool 'real' and how the LLM autonomously selects which one to invoke |
| **Zapier AI** | Build reliable trigger-based AI automations for consistent, repeatable workflows |
| **Error Handling** | Add max iterations, retry logic, and guardrail rules to make your agent production-safe |
| **Production Agent** | Ship a working research agent that searches, summarizes, saves, and handles edge cases gracefully |

---

### [Instructor says]

"Now let me give you the six key takeaways from today. I want you to write these down, because they're the mental models that will make you a better agent designer — in no-code *and* in code."

---

### Key Takeaways

> **[Instructor says — read each point slowly and pause briefly after each one]**

---

**1. Tools are what make agents real.**

"Without tools, an agent is just a chatbot talking to itself. Today, you connected real tools — web search, Google Docs, Gmail — and watched the agent use them autonomously. The LLM's intelligence is only as useful as the actions it can take."

---

**2. The agent decides which tool to use.**

"You didn't hardcode the sequence. You didn't write 'if research, then search; if save, then Docs.' The LLM read your goal, looked at the available tools, and figured out the sequence itself. *That* is the fundamental difference between an automation and an agent."

---

**3. Tool descriptions matter enormously.**

"The LLM picks tools based on their *descriptions* — the text you write next to each tool. A vague description leads to wrong tool selection. A clear, specific description leads to reliable behavior. This is prompt engineering applied to your architecture — not just your chat messages."

---

**4. Error handling is what separates demos from production.**

"Max iterations prevent infinite loops. Retries handle transient failures. System-message guardrails prevent hallucinated actions. Add all three to every agent you build. Not as an afterthought — as a design requirement."

---

**5. Start no-code, graduate to code.**

"n8n and Zapier let you prototype and ship agent workflows fast. When you hit their limits — and you eventually will — you'll have the design intuition to build in Python. And we'll cover that in the next classes. The patterns are identical. The syntax is just different."

---

**6. Your Class 7 paper spec maps directly to n8n nodes.**

"Your paper spec had: goal → system message, tools → connected nodes, memory → buffer, guardrails → settings. Today you built exactly that. You weren't just 'doing an exercise' last week. You were designing a real system. Now you know."

---

### [Transition]

"One last quiz before we close. This one's the big picture — and it's the question I'd ask in a technical interview if I wanted to know whether someone *understands* agents or just *uses* them."

---

---
## Quiz 5 — The Big Picture

> **[Instructor says]** "Final check. This one pulls everything from today together."

---

**You're designing an AI agent that will help a support team respond to customer tickets. A colleague suggests: 'Let's give the agent access to every tool we have — CRM, billing system, email, Slack, ticketing system, database, and our internal wiki — so it can handle anything.' What is the most important concern with this approach?**

- **A)** The agent will run too slowly because it has too many tools to choose from
- **B)** Giving the agent access to all tools removes human control — it could take unintended, irreversible actions (e.g., modifying billing records or sending emails without review)
- **C)** n8n cannot handle more than 5 tool connections on a single agent node
- **D)** The LLM will get confused by the tool descriptions and refuse to run

---

### Correct Answer: **B**

> **[Instructor says]** "B is the right answer — and if you got this one right, you're thinking like a production engineer, not just a builder.
>
> More tools = more autonomy = more risk of unintended actions. If you give an agent access to your billing system *and* let it run without guardrails, it might — with perfectly good intentions — modify a billing record incorrectly or send a mass email with wrong pricing information. These aren't hypothetical risks. They've happened in real deployments.
>
> The principle is called **minimal necessary permissions** — give the agent *exactly* the tools it needs for its specific job, nothing more. It's the same as the principle of least privilege in cybersecurity: don't give a service account admin rights when read-only is enough.
>
> A is partly true — more tools can slow decision-making slightly — but that's a performance concern, not a safety concern. Safety is primary.
> C is simply false — n8n supports many tool connections.
> D could occasionally happen with very similar tool descriptions, but it's solvable with better descriptions, not fewer tools.
>
> The right architecture: start with minimal tools. Add more only when you've verified the agent behaves reliably with what it has. Trust is earned incrementally — for agents just as for engineers."

---

---
## Closing — What's Coming Next

---

### [Instructor says]

"You just built your first production-grade AI agent. From scratch. With real tools, real error handling, and real outputs.

Take a moment to appreciate what that means. A few weeks ago, you were learning what a prompt was. Today you built an autonomous system that searches the web, writes documents, and handles failures gracefully — without you touching it after you press 'run.'

That's not a small step. That's the leap from *understanding AI* to *building with AI*.

In the next class, we're going to take everything you just learned — the architecture decisions, the tool design thinking, the error handling patterns — and implement them in Python. The no-code canvas will become code. The nodes will become functions. The connections will become function calls.

But the design? The design is exactly the same. You already know it.

See you in the next class. Go break something interesting."

---

### Quick Reference: Key Terms from Today

| Term | Plain-English Definition | Example |
|:---|:---|:---|
| **ReAct Loop** | Think → Act → Observe cycle that agents repeat until task is done | Agent thinks 'I need to search', searches, reads result, then decides next step |
| **Tool** | Any function the LLM can call to interact with the outside world | Web search, send email, create Google Doc |
| **Trigger** | The event that starts an automation workflow | New email arrives, user sends a chat message |
| **System Message** | Permanent instruction set for the agent — its rulebook | 'Always cite sources. Never create docs unless asked.' |
| **Window Buffer Memory** | Agent remembers the last N messages, forgets the rest | Like a log file with a rolling retention policy |
| **Max Iterations** | Hard cap on how many tool calls the agent can make per task | Circuit breaker for runaway loops |
| **Exponential Backoff** | Wait longer with each retry — 1s, 2s, 4s, 8s | Used in TCP, AWS SDK, and now your n8n agents |
| **Least Privilege** | Give the agent only the tools it actually needs | Don't give a research agent billing system access |
| **Hallucination** | LLM confidently generates incorrect or fabricated information | Agent invents revenue figures when search returns nothing |
| **Observability** | Ability to see what the agent did and why | n8n's execution panel showing every tool call |
"""