node_modules/
.next/
dist/
*.tsbuildinfo
next-env.d.ts

# generated by apps/web/scripts/sync-static-assets.mjs
apps/web/public/dotlottie-player.wasm

.env
.env.local

.graphify-out/
shots/
.superpowers/

graphify-out/

# throwaway verification scripts — never commit these
tmp-*.mjs
*_tmp.mjs

.voice-clips/
# the app's own copy of each clip it has fetched from the bucket, and the folder
# `voice:verify --isolate` parks the rendered clips in while it proves S3 is live
.voice-cache/
.voice-clips.parked/

# One JSON file per authoring run: which stage it reached, what each agent returned,
# how many revision cycles it burned. Local operational state, like a log — the
# artifacts a run produces are committed to its branch instead.
.authoring-runs/
