Use for hero stat blocks. Numbers carry weight — show them large, specific, undecorated.

```jsx
<Stat value="13×" label="Salary jumps" />
<Stat value="189" label="AI projects shipped" tone="brand" />
```

The numeral is Plus Jakarta Sans 700 — never Clash Grotesk (Clash is headlines only).
